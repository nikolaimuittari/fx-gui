{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Create Robot Python virtual environment",
            "command": "python3 -m venv .venv && source .venv/bin/activate && pip3 install -r requirements.txt && rfbrowser init",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}/"
            },
            "problemMatcher": []
        },
    ]
}
